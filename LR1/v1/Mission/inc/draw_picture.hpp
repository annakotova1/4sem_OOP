#ifndef DRAWPICTURE_H
#define DRAWPICTURE_H

#include <QGraphicsItemGroup>
#include "my_points_list.hpp"
#include "my_camera.hpp"

int draw_picture(all_points &object, my_camera &camera, QGraphicsItemGroup *group);

// Функция поиска координат точки
// Функция поиска координат точек
// Функция поиска плоскости, в которой лежит точка
// Функция поиска пересечения прямой и плоскости 
// Функция построения плоскости по перпендикуляру и точки плоскости
// Функция построения прямой по направляющему вектору и точке на прямой

// Функция отрисовки точки
#endif